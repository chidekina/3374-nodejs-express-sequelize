const { Router } = require('express');
const PersonController = require('../controllers/PersonController.js');
const { route } = require('../app');

const router = Router();

router.get('/peoople', Pe)